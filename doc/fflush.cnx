fflush -- 将缓冲内容输出到文件

  bool fflush(resource handle)~

本函数强制将所有缓冲的输出写入 {handle}
文件句柄所指向的资源。 成功时返回 TRUE，
或者在失败时返回 &false;.

文件指针必须是有效的，必须指向由 |fopen| 或 |fsockopen|
成功打开的文件(并还未由 |fclose| 关闭)。

vim:ft=help:
