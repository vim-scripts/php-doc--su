imagesx -- 取得图像宽度

  int imagesx(resource image)~

|imagesx| 返回 {image} 所代表的图像的宽度。

使用 |imagesx|

<?php >
  // create a 300*200 image
  $img = imagecreatetruecolor(300, 200);
  
  echo imagesx($img); // 300
?>



参见 |imagecreatetruecolor|，|getimagesize| 和 |imagesy|。

vim:ft=help:
